CREATE TABLE `orderinfo` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `tradeDate` decimal(9,0) NOT NULL,
  `strategyOrderId` bigint NOT NULL,
  `basketId` bigint unsigned NOT NULL,
  `accountId` char(20) NOT NULL,
  `brokerId` int unsigned NOT NULL,
  `tradeAcc` varchar(50) NOT NULL,
  `stockCode` char(30) NOT NULL,
  `exchangeId` int NOT NULL,
  `strategyType` decimal(4,0) NOT NULL,
  `tag` varchar(128) NOT NULL DEFAULT '',
  `bidFirstPrice` double DEFAULT NULL,
  `askFirstPrice` double DEFAULT NULL,
  `first_time` double DEFAULT NULL,
  `back_time` double DEFAULT NULL,
  `finish_time` double DEFAULT NULL,
  `status` tinyint NOT NULL,
  `statueMsg` varchar(512) DEFAULT NULL,
  `quoteTime` datetime(3) DEFAULT NULL,
  `orderTime` datetime(3) DEFAULT NULL,
  `orderPrice` decimal(10,3) NOT NULL,
  `orderVol` decimal(10,0) NOT NULL,
  `bsFlag` tinyint NOT NULL,
  `localId` char(100) DEFAULT NULL,
  `sysno` char(32) NOT NULL,
  `tradePrice` decimal(10,5) NOT NULL COMMENT 'tradePrice ',
  `tradeVol` decimal(10,0) NOT NULL,
  `cancelVol` decimal(10,0) NOT NULL DEFAULT '0',
  `cancelNum` int DEFAULT NULL,
  `remark` varchar(256) NOT NULL DEFAULT '',
  `createTm` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  `updateTm` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `channelId` varchar(32) NOT NULL DEFAULT '',
  `busStrategyOrderId` varchar(64) DEFAULT '',
  `tradeNo` varchar(64) DEFAULT '',
  `tradeAmt` decimal(20,3) DEFAULT '0.000',
  `tradeTm` bigint DEFAULT NULL,
  `mark1` bigint DEFAULT '0',
  `mark2` varchar(128) DEFAULT '',
  `start_push` tinyint(1) DEFAULT '0',
  `department` varchar(100) DEFAULT '',
  PRIMARY KEY (`id`),
  UNIQUE KEY `localId` (`localId`),
  KEY `date_account` (`tradeDate`,`accountId`)
) ENGINE=InnoDB AUTO_INCREMENT=28478 DEFAULT CHARSET=utf8mb3;